<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- This group is an abstract for all permissions -->
        <record id="group_rem_user" model="res.groups">
            <field name="name">User</field>
            <field name="implied_ids" eval="[(4, ref('product.group_uom'))]"/>
            <field name="category_id" ref="base.module_category_hidden"/>
        </record>

        <!-- Redefinition of the Salesman for including our group_rem_user -->
        <record id="group_sale_salesman" model="res.groups">
            <field name="name">User: Own Leads Only</field>
            <field name="category_id" ref="base.module_category_sales_management"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user')),(4, ref('rem.group_rem_user'))]"/>
            <field name="comment">the user will have access to his own data in the sales application.</field>
        </record>

        <record id="group_use_buyer_contracts" model="res.groups">
            <field name="name">Use buyer contracts</field>
            <field name="category_id" ref="base.module_category_hidden"/>
        </record>

    </data>
</odoo>
